import { useState } from "react";
import { useCare } from "@/lib/care/store";
import { AccommodationSelector, type AccommodationValue } from "@/components/maintenance/AccommodationSelector";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { toast } from "sonner";
export function ResidentAccommodationDialog({residentId,trigger}:{residentId:string;trigger?:React.ReactNode}){const care=useCare();const resident=care.residents.find(r=>r.id===residentId);const [open,setOpen]=useState(false);const room=care.rooms.find(r=>String(r.id)===String(resident?.roomId));const [value,setValue]=useState<AccommodationValue>({nursingHomeId:resident?.facilityId||care.activeFacilityId,wingId:room?.wingId||resident?.wingId||"",roomId:String(room?.id||""),bedId:""});return <Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild>{trigger||<Button>Assign Bed</Button>}</DialogTrigger><DialogContent className="max-h-[90vh] max-w-3xl overflow-y-auto"><DialogHeader><DialogTitle>Resident Accommodation</DialogTitle></DialogHeader><AccommodationSelector value={value} onChange={setValue}/><DialogFooter><Button variant="outline" onClick={()=>setOpen(false)}>Cancel</Button><Button disabled={!value.bedId} onClick={()=>{try{care.assignResidentToMaintenanceBed(value.bedId,residentId,new Date().toISOString());toast.success("Bed assigned and resident accommodation updated.");setOpen(false)}catch(e){toast.error(e instanceof Error?e.message:"Bed assignment failed. Select another Bed.")}}}>Confirm Assignment</Button></DialogFooter></DialogContent></Dialog>}
