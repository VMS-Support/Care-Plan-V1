# Rule Clinical Approval

Clinical rules cannot activate unless `clinicalApproval.status` is `approved` or `not_required`, an effective date exists, output definitions have dedupe scope and explanations are complete. Weight loss, missed-care and care-plan coverage reference rules remain pending approval.
