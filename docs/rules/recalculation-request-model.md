# Recalculation Request Model

`RuleRecalculationRequest` records mode, reason, scope, rule/version, requester, approver, hashes, correlation ID and summary. `RuleRecalculationItem` records deterministic item ID, rule version, source event, engine version, source hash and status.
