# Rule Configuration

Configuration is stored on the rule definition, separate from evaluator code. Units and periods are explicit. Clinical-threshold defaults do not imply approval. Home-specific overrides use `nursingHomeId` and are sorted ahead of global rules.
