# Rule Output Types

Controlled output types are clinical alert, recommendation, review requirement, follow-up task, risk update, care-plan coverage gap, dashboard signal and report flag. Rule code proposes outputs; typed handlers persist deduplicated records.
