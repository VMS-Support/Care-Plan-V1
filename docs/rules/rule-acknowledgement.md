# Rule Acknowledgement

`acknowledgeRuleIssue(issueId, context, note?)` records actor, timestamp, scope and optional note. It requires `rule_issue.acknowledge`, active episode status and same-home access. It does not resolve or delete the issue.
