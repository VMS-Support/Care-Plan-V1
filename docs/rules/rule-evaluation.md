# Rule Evaluation

`evaluateRule` returns a deterministic `RuleEvaluationResult` with decision ID, status, matched conditions, source references, proposed outputs and explanation. `evaluateEventAgainstRules` uses registry selection and declared providers. `evaluateRuleFixture` supports simulation without persistence.
