# Rule Issue Episodes

The chosen model is one stable RuleIssue per deduplication key with multiple episodes. A recurring issue opens a new `RuleIssueEpisode` while preserving the prior closed episode.
