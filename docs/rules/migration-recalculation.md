# Migration Recalculation

Migration should create structured source records or synthetic recalculation inputs, then enqueue home-scoped recalculation. Backfill must be chunked, resumable and deterministic.
