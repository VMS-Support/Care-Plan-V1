# Recalculation Operations

Track queue depth, running requests, item failures, retry counts, per-rule processing and cross-home errors. Cancellation stops unstarted work and preserves completed transitions.
