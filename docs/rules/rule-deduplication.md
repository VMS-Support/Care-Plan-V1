# Rule Deduplication

Outputs carry deterministic deduplication keys. Supported scopes include event, resident, resident and code, resident domain and home. Existing active outputs are updated with occurrence count/source references instead of duplicated.
