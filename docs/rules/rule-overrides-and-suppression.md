# Rule Overrides And Suppression

`RuleOverride` and `RuleSuppression` require scope, reason, creator and optional expiry. Future UI should require audit reasons for override, suppression, dismissal and resolution.
