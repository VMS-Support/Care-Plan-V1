# Rule Resolution

Resolution requires a code and reason. Automatic resolution is allowed only when a rule output has an approved resolution policy. Manual resolution closes the active episode and preserves source decisions.
