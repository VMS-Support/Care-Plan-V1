# Rule Definition Model

`RuleDefinition` captures ID, version, status, category, trigger events, optional nursing-home scope, severity, configuration, required data, output definitions, effective dates, clinical approval and explanation template.

Activated rules are immutable. A configuration change creates a new version; historical decisions retain the evaluated version.
