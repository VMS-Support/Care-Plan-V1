# Handover Acknowledgement

Acknowledgement is explicit and user-specific. Opening a handover page or recalculating unread counts does not acknowledge anything.

The legacy `acknowledgedBy` and `acknowledgements` arrays are retained. New acknowledgements also add `handoverAcknowledgements` with user account, staff, shift, home, ward and timestamp.
