# Handover Shift Policy

New handovers default source shift to the current operational context and target shift to the next configured shift for the selected nursing home.

Legacy labels map as:

- `morning` to Day Shift;
- `afternoon` to Late Shift;
- `night` to Night Shift.

Night-to-day handovers use the operational date policy from the shift model.
