# Work Item Parallel Run

The current phase is shadow-only: existing UI reads remain authoritative. Compare legacy and projected sets by stable source occurrence, not title. Required reports cover active/overdue Care Actions, Next 4 Hours, tasks due, assessment reminders, observation reminders, resident/ward scope, status, and duplicates.

Gates for each queue are equal item set, order, window boundaries, labels, selected-ward scope, home-wide deduplication, routes, completion workflow, and zero critical integrity issues. Upcoming Care Interventions must retain grouping, due descriptions, resident/room, Open Resident, and Care Action completion. Next 4 Hours must retain its current no-prior-completion behaviour and exclude completed/discontinued sources.

Current result: infrastructure and deterministic comparison keys are ready; no production data store/backfill exists, so live migration counts and performance-at-3,000-residents are not claimed. The validator reports this as an explicit limitation rather than fabricating parity.
