# Ward and Shift Permissions

Relevant capabilities include `home.switch`, `ward.view`, `ward.switch`, `ward.select_multiple`, `ward.view_all_authorised`, `ward.override`, `shift.view` and `shift.change_context`.

The current component uses canonical home assignment, ward competency and scoped role assignment. Future explicit grants can widen or restrict this without changing the UI component.
