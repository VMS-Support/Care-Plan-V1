# Ward Selection

Ward selection supports:

- `single`;
- `multiple`;
- `all_authorised`.

`All Wards` means all authorised wards only. Empty `wardIds` is not global access. Nurse/HCA defaults to a single approved ward. CNM and DON can select broader authorised ward sets.
