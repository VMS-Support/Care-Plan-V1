# Shift Definition Model

`ShiftDefinition` belongs to a nursing home and includes stable ID, label, start local time, end local time, active flag and display order.

Default migrated labels preserve the current app language: Day Shift, Late Shift and Night Shift. Different homes may later define different labels and boundaries.
