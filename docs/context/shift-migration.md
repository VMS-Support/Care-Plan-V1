# Shift Migration

The migration is additive. Each nursing home receives central shift definitions if none exist.

Current labels are preserved: Day Shift, Late Shift and Night Shift. Legacy string-based shift values remain readable during transition.
