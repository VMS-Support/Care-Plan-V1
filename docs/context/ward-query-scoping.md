# Ward Query Scoping

Operational queries treat selected ward IDs as a set and deduplicate by entity ID.

Implemented context selectors cover residents, care actions, tasks, observations, alerts, handovers and incidents. Future-only contracts are documented for appointments, admissions and returns.
