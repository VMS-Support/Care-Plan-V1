# Multi-Ward Selection

Multi-ward selection is allowed only when the context service confirms the active role and authorised ward set permit it. CNM and DON style management scopes can select multiple wards or All Authorised Wards.

All Authorised Wards means exactly the active wards the user can access inside the selected nursing home. It never means all wards globally and never uses an empty ward list.
