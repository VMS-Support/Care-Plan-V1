# Event Payload Standard

Payloads contain stable identifiers and structured values needed by subscribers. They do not contain full clinical records, full narrative notes, full assessment answers or large repeated free text.

The source clinical entity remains the source of truth. Events announce that something happened and provide enough references for consumers to load source data if they are authorised.
